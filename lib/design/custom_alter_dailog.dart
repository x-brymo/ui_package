import 'package:flutter/material.dart';

class CustomAlterDialog extends StatelessWidget {
  const CustomAlterDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(),
    );
  }
}
